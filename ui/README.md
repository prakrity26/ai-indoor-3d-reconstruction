# ui

Streamlit client for operators and demos.

**Phase:** 13  
**Status:** placeholder

This package will upload video, poll job status, and embed an interactive 3D viewer. It must call the API only. It must not import `model/` or run reconstruction locally.

Until Phase 13, `splat_viewer.html` is a **local test page** in this same repo: open it in a browser and load `point_cloud.ply` from Colab.
